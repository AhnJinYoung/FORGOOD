# ================================================================
#  FORGOOD — Environment Variables
#  Copy this file:  cp .env.example .env
#  Then fill in your values.
# ================================================================

# ── Database ─────────────────────────────────────────────────
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
DB_NAME=forgood

# ── API Mode ─────────────────────────────────────────────────
# "test"    → free AI (openrouter/auto), seed data on boot
# "serving" → production AI (kimi-k2.5), no auto-seed
FORGOOD_MODE=serving

# ── Domain / URL ─────────────────────────────────────────────
# Your EC2 public domain or IP (used in nginx & CORS)
API_DOMAIN=api.forgood.xyz
# Your Vercel frontend URL (used for CORS)
SITE_URL=https://forgood.vercel.app
# CORS_ORIGIN — comma-separated list of allowed origins
CORS_ORIGIN=https://forgood.vercel.app,https://forgood.xyz

# ── AI (OpenRouter) ─────────────────────────────────────────
# Get your key at https://openrouter.ai/keys (one key for all models)
OPENROUTER_API_KEY=sk-or-v1-YOUR_KEY_HERE

# Optional: override models (defaults are fine)
# OPENROUTER_MODEL=moonshotai/kimi-k2.5
# OPENROUTER_MODEL_VISION=moonshotai/kimi-k2.5

# ── On-Chain (Monad Testnet) ────────────────────────────────
# Deploy contracts first: bash scripts/deploy-contracts.sh
# Then paste values from .env.contracts
AGENT_PRIVATE_KEY=0xYOUR_AGENT_WALLET_PRIVATE_KEY
TREASURY_ADDRESS=0xDEPLOYED_TREASURY_ADDRESS
FORGOOD_TOKEN_ADDRESS=0xDEPLOYED_TOKEN_ADDRESS
REGISTRY_ADDRESS=0xDEPLOYED_REGISTRY_ADDRESS
AUTO_PAYOUT=true

# ── Vercel Environment Variables (set in Vercel dashboard) ───
# These are NOT needed in this .env file — they are set in
# the Vercel project settings:
#   NEXT_PUBLIC_API_URL=https://api.forgood.xyz
#   NEXT_PUBLIC_TREASURY_ADDRESS=0x...
#   NEXT_PUBLIC_TOKEN_ADDRESS=0x...
#   NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id
